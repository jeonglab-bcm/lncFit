submitter: claude (CI dry run)
model: constant base-rate baseline
description: >
  Not a real model -- every row predicted at the fixed 5.3% base rate.
  Submitted only to exercise the leaderboard CI end-to-end (validation +
  independent scoring + auto-commit of LEADERBOARD.md). Safe to close/delete
  this test PR and branch once verified.

